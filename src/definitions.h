#pragma once
#include <iostream>
#include <string>
#include <vector>

using std::string;
using letter = char;
using real = double;
using std::cerr;
using std::cout;
using std::exception;
using std::size_t;
using std::vector;

namespace po = boost::program_options;